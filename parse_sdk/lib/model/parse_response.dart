class ParseResponse {
  // String objectId;
  // DateTime createdAt;
  // DateTime updatedAt;
  ParseResponse({
    required String objectId,
    required DateTime createdAt,
    required DateTime updatedAt,
  });
}
